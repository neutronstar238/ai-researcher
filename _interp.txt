VERDICT: claim_not_supported

SUPPORTS:
The candidate method (official-05-r2) does not improve over the baseline. The overall median log effect is -0.6859100612592094, indicating worse performance. The ODE stratum median is -0.017150454000234766 and the PDE stratum median is -15.41311654930732, both negative. The bootstrap 95% interval is [-2.059226697281019, 1.3231325603119128], which includes zero and negative values. The frozen gate failed: all_candidate_cells_succeeded is false, ode_stratum_non_negative is false, pde_stratum_non_negative is false, overall_median_at_least_minimum is false, and bootstrap_lower_above_zero is false.

DOES NOT SUPPORT:
The evidence does not support any claim that the candidate method improves prediction accuracy over the baseline across ODE or PDE systems. It does not support a claim of generalisation, robustness, or even parity with the baseline. The candidate failed entirely on reaction_diffusion_cylinder (0 of 6 cells succeeded, loss capped at 1e12), and performed worse than baseline on 7 of 12 systems.

COUNTER-READING:
The PDE stratum median of -15.41311654930732 is driven almost entirely by a single catastrophic failure on reaction_diffusion_cylinder (paired_log_effect of -29.51550192036587 due to 0/6 candidate cells succeeding), meaning the PDE result rests on only 2 systems and is not representative of PDE performance broadly. Additionally, the candidate was selected on 'median validation NMSE over executed cells, failures penalised', yet still failed all cells on one system, suggesting the selection criterion did not prevent overfitting to validation conditions that did not transfer to test.

LIMITATIONS:
- Only 2 PDE systems were tested, making the PDE stratum median highly sensitive to a single failure.
- The candidate failed all 6 cells on reaction_diffusion_cylinder, resulting in a capped loss of 1e12 that dominates the PDE stratum.
- The bootstrap lower bound of -2.059226697281019 is far below zero, indicating substantial uncertainty and possible severe degradation.
- 7 of 12 systems showed negative paired log effects, indicating the candidate was worse than baseline on a majority of systems.
- The selection basis penalised failures but the candidate still had a complete failure on one system, questioning the reliability of the selection process.